tcp:
  routers:
    tcp-router-2:
      rule: "HostSNI(`sandbox.ctf.minpeter.tech`)"
      service: tcp-service-2
      tls: {}
  services:
    tcp-service-2:
      loadBalancer:
        servers:
          - address: "host.docker.internal:5556"  # Replace with your TCP server address
