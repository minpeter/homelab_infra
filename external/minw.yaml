tcp:
  routers:
    tcp-router:
      rule: "HostSNI(`minw.ctf.minpeter.tech`)"
      service: tcp-service
      tls: {}
  services:
    tcp-service:
      loadBalancer:
        servers:
          - address: "host.docker.internal:5555" # Replace with your TCP server address
